#include <stdio.h>

int main
{
	printf("El tamaño de la variables de coma flotante es %d", size);
	printf(" bytes\n ");
    printf("El tamaño de la variables de doble precision  es %d", size);
    printf(" bytes\n ");
    return 0;
}
